{
  "name": "couple-os-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Couple OS — native shells (Windows EXE / macOS / Android APK-AAB) via Tauri v2",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build",
    "android:init": "tauri android init"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.7"
  }
}
